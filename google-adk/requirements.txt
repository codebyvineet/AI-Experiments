# FastAPI and web framework
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.7.4
pydantic-settings>=2.1.0

# Database drivers
motor>=3.3.2
pymongo>=4.6.1
redis>=5.0.1

# Google ADK (Agent Development Kit) — replaces custom LangGraph agent
google-adk>=1.0.0

# ADK MongoDB Session Service — framework-native persistent session storage
# (analogous to LangGraph's MongoDBSaver checkpointer)
adk-mongodb-session>=1.0.0

# Google Generative AI (Gemini — used by ADK internally)
google-generativeai>=0.8.0

# MCP server (standalone — kept separate per project constraint)
# Security-patched versions: fixes DNS rebinding and DoS vulnerabilities
mcp>=1.23.0
fastmcp>=2.0.0

# Authentication
python-jose[cryptography]>=3.4.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1

# Utilities
python-dotenv>=1.0.1
httpx>=0.26.0
# Security patch: fixes arbitrary file write and DoS vulnerabilities
python-multipart>=0.0.22
