# MCP server dependencies (standalone container)
uvicorn[standard]>=0.27.0
fastapi>=0.104.0

# MCP server (security-patched)
mcp>=1.23.0
fastmcp>=2.0.0

# HTTP client for proxying to backend API
httpx>=0.27.0

# Utilities
python-dotenv>=1.0.1
# Security patch: fixes arbitrary file write and DoS vulnerabilities
python-multipart>=0.0.22
